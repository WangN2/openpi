# 学习日志

## 2026-09-10：补齐训练与评测依赖

- 主项目 `.venv`：Python 3.11.15，uv 锁文件检查无变化，240 个包兼容；JAX 0.5.3 和 PyTorch 2.7.1 均识别 CUDA 设备。
- 完整 LeRobot LIBERO：固定版本 `a4336d589d...`，34.94 GB；1693 个 episode 加 6 个仓库/元数据文件在服务器端 1699/1699 通过 SHA-256。
- 数据加载实测：1693 episodes、273465 帧、40 个任务；batch 2 输出三路 `224×224` 图像、32 维状态、200 token 和 `2×10×32` 动作。
- 补齐 PaliGemma tokenizer，并让 `pi05_libero` 与低显存配置成功加载官方 `state`、`actions` 归一化统计。
- 新增 `pi05_libero_low_mem_finetune` LoRA 配置；实测 batch 32、8 OOM，batch 4 成功完成一次参数更新，loss `0.06416`、grad norm `0.8708`、param norm `1803.77`。
- LIBERO 独立 Python 3.8.20 评测环境共 125 个兼容包；实测成功创建第一个 Spatial 任务、载入官方初始状态、取得主视角和腕部 `256×256` 图像并执行一步动作。
- 官方 `pi05_libero` 权重端到端推理成功，返回 `(10, 7)` action chunk 且数值全部有限；首次加载约 26 秒，首次 JAX 编译加推理约 30 秒。
- 容器未暴露可用的 NVIDIA EGL 图形设备，评测改用 Mesa 软件 EGL 无头渲染；模型推理仍可使用 CUDA。
- 本次工作是环境准备证据，不替代学习者完成 L02 的概念与目录复述；总进度保持 1/24。

## 2026-09-08：L01 验收通过，开始 L02

- 学习者能指出 VLA 使用本体状态和多路视觉，输出 action chunk；补充输入还包括语言指令，action chunk 是未来若干步的低层动作序列。
- 学习者正确区分推理与微调：推理不更新模型参数，微调会通过优化器更新可训练参数。
- 学习者指出训练 loss 下降不能确认场景泛化；补充 loss 是示范数据上的代理指标，还需固定验证集、仿真或真机成功率检验。
- 验收结果：L01 已通过；总进度 1/24（4.2%）；进入 L02。
- L02 现场状态：Ubuntu 22.04.4、系统 Python 3.10.12、项目固定 3.11、uv 0.11.8、A100-SXM4-40GB；子模块已初始化；`.venv` 不存在；`OPENPI_DATA_HOME` 当前未设置。
- 下一步：学习者观察并解释系统环境和项目环境的区别，再建立 `.venv`、同步依赖并完成最小导入检查。

## 2026-09-08：完成课程资源准备

- 已准备官方 JAX 权重：`pi05_base`（后续微调起点）和 `pi05_libero`（推理、评估、对照）。
- 已准备入门训练数据：LIBERO Spatial no-noops 原始 RLDS，包含 16 个 TFRecord 分片和 2 个元数据文件。
- 校验结果：两套权重共 45 个对象与本地中转副本逐文件 SHA-256 一致；数据 18 个文件均与发布方 LFS SHA-256 一致。
- 网络中断产生的残缺文件已移动到 `/ephstorage/openpi_data/incomplete/20260908`，不会进入课程加载路径。
- 仓库仍位于 `feature/learning_dev`，提交 `0b061a667442`。
- 课程进度保持 L01 学习中，0/24；资源准备不是学习验收。
- 下一步：完成 L01 三项概念复述，然后在 L02 建立并验收 Python 3.11 项目环境。

## 2026-09-07：建立课程与初始位置

- 学习者自述：会 Python，没有训练深度学习模型的经验，最终希望控制真实机器人。
- 已完成助手工作：核对官方仓库来源、π0.5 实现和配置；预检服务器 GPU、磁盘及 Python；建立 8 阶段 24 课路线和验收规则。
- 已提供讲解：VLA 的图像、语言、状态输入与动作输出；推理、训练、微调区别。
- 学习者动手记录：暂无。
- 验收结果：尚未验收；L01 学习中；课程通过 0/24。
- 待确认：真实机器人与相机型号、控制接口；实际可用 GPU 显存；项目环境和权重。
- 下一步：完成 L01 三项概念复述，再进入 L02。

## 后续每课记录模板

- 日期 / 课次：
- 本课目标：
- 讲解内容：
- 学习者实际完成的练习：
- 实验环境、配置、数据划分、种子及日志/权重路径（适用时）：
- 验收方式与证据：
- 结果：已通过 / 待验收 / 需复习 / 待资源
- 错误与解决方法：
- 需要复习的概念：
- 下一次起点：
