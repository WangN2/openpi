# 当前学习位置

更新日期：2026-09-08

**当前位置：S0 定位与准备 → L02 服务器、环境与项目结构（学习中）。**

- 课程验收进度：1 / 24 课（4.2%）。
- 阶段验收：0 / 8。
- 最近目标：区分系统 Python 与项目环境，建立 `.venv`，并能说明 OpenPI 的主要目录。
- 下一个里程碑：M1——独立运行并解释一个小模型训练循环。

## 已确认与待确认

| 项目 | 当前情况 |
|---|---|
| 学习基础 | 会 Python，没有深度学习训练经验 |
| 最终目标 | 控制真实机器人 |
| 仓库 | `rt_dev:/ephstorage/openpi`；当前分支 `feature/learning_dev` |
| main 同步 | 个人仓库 `main` 已与官方 `main` 对齐到 `215abfb217db`；学习分支已 rebase |
| π0.5 实现 | 已核对 JAX/PyTorch pi05 分支与训练配置；公开版本支持 flow matching 动作头 |
| GPU | A100-SXM4-40GB，驱动 535.154.05；当前账号无权读取实时显存数字 |
| 磁盘 | 准备资源后 `/ephstorage` 约剩余 122GB |
| 环境 | Ubuntu 22.04.4；系统 Python 3.10.12；项目固定 3.11；uv 0.11.8 已找到 Python 3.11；`.venv` 尚未建立 |
| 模型 | 官方 `pi05_base` 与 `pi05_libero` 已准备并通过逐文件 SHA-256 核对 |
| 数据 | LIBERO Spatial RLDS 已准备：16 个 TFRecord 分片、2 个元数据文件，均通过发布方 SHA-256 校验 |
| 实际运行 | 尚未加载模型或启动训练；这是 L02-L06 的动手内容 |
| 硬件 | 机械臂、夹爪、相机、控制接口待学习者提供 |

资源的准确路径、用途和校验方式见 `learning/资源清单.md`。

## 课次状态

| 课次 | 主题 | 状态 | 验收证据 |
|---|---|---|---|
| L01 | VLA 与训练基本概念 | 已通过 | 学习者能说明视觉/本体输入、action chunk 输出、推理与微调参数变化及泛化问题；补充语言指令和 loss 的代理指标性质后通过 |
| L02 | 环境与项目结构 | 学习中 | 已盘点 OS、Python、uv、GPU、子模块、数据缓存和磁盘；`.venv` 待建立，项目结构待学习者复述 |
| L03 | 张量与设备 | 未开始 | — |
| L04 | 前向计算与损失 | 未开始 | — |
| L05 | 梯度与训练循环 | 未开始 | — |
| L06 | 加载配置与权重 | 未开始 | — |
| L07 | π0.5 输入输出 | 未开始 | — |
| L08 | 重复推理与计时 | 未开始 | — |
| L09 | 调用链 | 未开始 | — |
| L10 | 模型组成 | 未开始 | — |
| L11 | Flow matching | 未开始 | — |
| L12 | π0.5 配置 | 未开始 | — |
| L13 | 示范数据结构 | 未开始 | — |
| L14 | 转换与归一化 | 未开始 | — |
| L15 | 小样本训练检查 | 未开始 | — |
| L16 | 微调与恢复 | 未开始 | — |
| L17 | 评估基线 | 未开始 | — |
| L18 | 模型对比 | 未开始 | — |
| L19 | 排错 | 未开始 | — |
| L20 | 真机硬件接口 | 未开始 | 硬件待确认 |
| L21 | 观察动作对齐 | 未开始 | — |
| L22 | 不执行动作的联调 | 未开始 | — |
| L23 | 真机简单任务 | 未开始 | — |
| L24 | 独立闭环 | 未开始 | — |

## 下一次学习的起点

继续 L02：学习者先登录服务器、进入仓库，观察当前目录、分支、系统 Python、项目固定 Python 和 uv 找到的 Python 3.11，并解释为什么这些结果不同。随后建立 `.venv`、同步依赖，验证 Python、JAX 和项目导入，最后说明 `src/openpi/models`、`training`、`policies`、`scripts` 与 `examples` 的职责。

验收：能独立进入正确分支；能区分系统 Python、项目 Python、`pyproject.toml` 和 `uv.lock`；能说明至少三个核心目录；项目环境通过最小导入检查。

## 更新约定

每次开课读取本文件；每次结课更新对应课次、证据、统计和下一次起点，并追加学习日志。仅在学习发生时更新，不承诺后台自动跟踪。续接新会话时提供服务器 `learning/` 路径即可恢复位置。
